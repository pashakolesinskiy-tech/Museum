# Улучшения для сайта Музей детского сада №24

## 1. performance — изображения

**Проблема:** Все изображения грузятся оригинального размера. Нет lazy loading для карусели на главной.

| Улучшение | Эффект |
|-----------|--------|
| Добавить `loading="lazy"` на `carousel-item img` (index.html:330-361) | Ускорит FCP |
| Конвертировать `.png` в `assets/awards/` в `.webp` (award-06..09) | -40% размер |
| Добавить `<link rel="preload">` для hero-изображения | Мгновенная отрисовка |
| Убрать `background-attachment: fixed` на десктопе (style.css:45) | Мобильная производительность |

## 2. UX — лайтбокс и навигация

| Улучшение | Файл |
|-----------|------|
| Лайтбокс: добавить свайп влево/вправо на мобильных | script.js |
| Лайтбокс: добавить счётчик "3 / 19" | script.js |
| Навигация: добавить текущую страницу в хедер (portnova, gallery и т.д.) | script.js renderHeader() |
| Хедер: скрывать при скролле вниз, показывать при скролле вверх | script.js |

## 3. SEO и accessibility

| Улучшение | Файл |
|-----------|------|
| Добавить `lang="ru"` на все HTML (уже есть) | OK |
| Добавить `<meta name="theme-color">` на все страницы | portnova.html, gallery.html и др. |
| Добавить `aria-current="page"` в хедер для текущей страницы | script.js |
| Добавить `rel="canonical"` на все страницы | Все HTML |
| Исправить Schema.org URL: `https://example.com` → реальный домен | gallery.html:48 |

## 4. Код — дублирование

| Проблема | Решение |
|----------|---------|
| `renderGrid()` больше не используется (все заменены на `loadGalleryManifest`) | Удалить `renderGrid` из script.js |
| `renderHeader()` и `renderFooter()` вызываются синхронно, но используют `innerHTML` | Оставить как есть (OK) |

## 5. Безопасность и стабильность

| Улучшение | Файл |
|-----------|------|
| Добавить `vercel.json` с заголовками кеша для статики | Новый файл |
| Добавить `Content-Security-Policy` мета-тег | index.html |
| Видео-галерея: inline JSON осталась в `video-gallery.html:62-116` | Перевести на манифест |

## Приоритеты

1. **Высокий:** Удалить мёртвый `renderGrid`, перевести video-gallery на манифест
2. **Средний:** Лайтбокс-улучшения (свайп, счётчик), canonical URL
3. **Низкий:** Предзагрузка, конвертация PNG→WebP, CSP

## Верификация
1. `script.js` — нет ошибок, `renderGrid` удалён
2. `video-gallery.html` — работает с манифестом
3. Все страницы загружаются без ошибок в консоли
4. Лайтбокс работает с свайпом на мобильных
